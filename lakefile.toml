name = "Otriangle"
version = "0.1.0"
keywords = ["math"]
defaultTargets = ["Otriangle"]
precompileModules = false

[leanOptions]
pp.unicode.fun = true # pretty-prints `fun a ↦ b`
relaxedAutoImplicit = false
weak.linter.mathlibStandardSet = true
maxSynthPendingDepth = 3
experimental.module = true

[[require]]
name = "VersoBlueprint"
git = "https://github.com/leanprover/verso-blueprint"
rev = "v4.32.0"

# Keep Mathlib last so its dependency revisions take precedence when Lake
# reconciles packages shared with Verso Blueprint (notably ProofWidgets).
[[require]]
name = "mathlib"
scope = "leanprover-community"
rev = "v4.32.0"

[[lean_lib]]
name = "Otriangle"
globs = ["Otriangle", "Otriangle.Basic", "Otriangle.Comparator.Defs", "Otriangle.LCFT", "Otriangle.MonoAnabelian.+", "Otriangle.OTriangle", "Otriangle.OTriangle.Core"]

# The comparator challenge and its solution.  Kept out of `defaultTargets` because
# `Otriangle.Comparator.Challenge` is `sorry` by construction.  The definitions they share live in
# `Otriangle.Comparator.Defs`, which belongs to the main library above.
[[lean_lib]]
name = "OtriangleComparator"
roots = ["Otriangle.Comparator.Challenge", "Otriangle.Comparator.Solution"]
globs = ["Otriangle.Comparator.Challenge", "Otriangle.Comparator.Solution"]

[[lean_lib]]
name = "OtriangleBlueprint"
roots = ["Otriangle.Blueprint"]
globs = ["Otriangle.Blueprint", "Otriangle.Chapters.+"]
