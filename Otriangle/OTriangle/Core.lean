import Otriangle.Comparator.Defs

/-!
# Core definitions for the O-triangle formalization

The local-field presentations, the Galois-group and Galois-monoid categories, and their forgetful
functors live in `Otriangle.Comparator.Defs`, which imports only Mathlib so that it can serve as
the trusted common ancestor of the comparator challenge and its solution.  This module re-exports
them under their historical location; importing it is equivalent to importing that module.
-/
